# ノート形式 v2

`## Parent` / `## Child` の見張り行だけで、「こっちにとって（自分視点）」と
「そっちにとって（相手視点）」を分ける方式（旧 v1 の `Parent:` / `Child:` /
`BackLink:` 見出しとは別物 ── コロンも中身の直書きも無く、見張りだけの行）。
見張りは Markdown の H2（`##`）にしてあるので、そのまま見出しとして表示される。

実装: `plugin/yurii_PKM/python/note_format_v2.py` + `autoload/yurii_pkm.vim`。
既定でこれ。旧エンジンに固定したい時だけ `let g:yurii_pkm_format = 'v1'`。

---

## 1. ノート 1 枚の形

```markdown
---
time: 2026-09-09 14:12:00
title: 認識論とは何か
attribute: グループ        ← 任意。容器ノートの印だけ（§3）
---

# 認識論とは何か

本文（散文）。ここには `---` も `ワード:` も何でも書ける。

## Parent
所属: [哲学](20250101120000.md)
前提: [知識の定義](20250103120000.md)
論点:
[普遍は実在するか](20250111120000.md) — 実在論 vs 唯名論
[認識の起源](20250112120000.md)
## Child
論点: [実在論で見るべき](20250120120000.md)
関連: [存在論](20250121120000.md)
バックリンク: [2026-05 の読書メモ](20260501120000.md)
```

- ファイル名 = 14 桁タイムスタンプ。`index.md` だけ別。
- front matter は `time` / `title`（+ 任意で `attribute: グループ`）。
- 本文の後、`## Parent` 行から下が **上側**（こっちにとって＝このノートにとって、そのノートが○○）。
- `## Child` 行から下が **下側**（そっちにとって＝そのノートにとって、このノートが○○）。
- 見張りは行全体が `## Parent` / `## Child` だけの行（コロンも中身も無い）。
  `##` なしの旧 v2 見張り（`Parent`/`Child`）も読み込み時に認識し、次の sync で
  `##` 付きへ自動移行する。旧 v1 の `Parent:`/`Child:`（コロン付き・直後にリンク）
  とは別物なので混同しない。
- **見張り 2 行はノート作成時（テンプレート）にだけ入る。sync は後から足さない。**

## 2. 関係（relation）

見張りの下に入るセクション。1 行 1 リンク。

ピッカー（通常）: `1 ノート  2 補足  3 資料  4 関連  5 入力`

グループ / 小グループ ノードへリンクする時だけ、`ノート` の代わりに `索引` が出る:
`1 索引  2 補足  3 資料  4 関連  5 入力`（意味は同じ既定・無色の関係。§3）

`グループ` はどちらのピッカーにも出さない（選べると向きが固定されて壊れる。§3参照）。

| 関係 | 向き | 意味 |
|---|---|---|
| `グループ:` | こっちにとって のみ | 容器ノートへの所属。ピッカーでは選べず、相手が `attribute: グループ` / `attribute: 小グループ` なら sync が**自動でこのラベル**にする |
| `索引:` | 両方 | グループ / 小グループ ノードへリンクする時の既定（`ノート` の言い換え） |
| `ノート:` | 両方 | 既定。特別な役割のないリンク |
| `関連:` | 対称 | 横のつながり（両ノートの下側に出る） |
| `補足:` | 両方 | 本体を補う説明 |
| `資料:` | 両方 | 出典・参考にしたもの |
| 他の `語:` | 両方 | 自由入力 |

- **論点 / 見解 / 前提 は廃止**した。既存ノートの `論点:` / `見解:` / `前提:` は
  そのまま残り、sync の並び替えも従来どおり効く。ピッカーから選べなくなっただけ。
- 旧 `キーワード:`（「このノートを索引する語を自由入力する」関係）も廃止した。
  同じ役割は `attribute: 小グループ` のノードが引き継ぐ（§3）。既存ノートの
  `キーワード:` はそのまま残る（読み込み・並び替えは従来どおり）。

## 3. ノード属性（`attribute: グループ` / `attribute: 小グループ`）

容器ノート（グループ）・サブグループ（小グループ）の印。`zw` でのみ作れる
（属性の種類 → グループ以外なら関係 → c=子 / p=親、の順に聞く）。後から他の
ノートを属性化する専用コマンドは無い（front matter に手で
`attribute: 小グループ` を足せば効く）。

> 旧名との互換: 以前の `attribute: カテゴリー` / `attribute: キーワード` は
> それぞれ `グループ` / `小グループ` として引き続き認識される。front matter
> 自体を書き換える必要はない。

選んだ関係に関わらず、次の 2 つの自動ルールでラベルが上書きされる
（`グループ` と `小グループ` で規則が違う）:

1. 相手が `attribute: グループ` **または** `attribute: 小グループ` → 自分の
   こっちにとって 側は、値に関わらず常に `グループ: [相手]`
   （相手がサブグループだろうと容器だろうと、自分から見れば「そこに属している」
   ことに変わりはないため）。
2. 自分が `attribute: グループ` → 相手の そっちにとって 側が
   `グループ: [自分]`（サブ容器として）。
   **自分が `attribute: 小グループ` の場合はこの上書きをしない** ── 相手の
   そっちにとって には実際に選んだ関係名がそのまま並ぶ。小グループは
   「何に属しているか」を見せるだけで、相手を自分の下位に置く容器では
   ないため。

つまり非対称のしかたが両者で違う:

- **グループ**: 容器ノード自身の そっちにとって には実際に選んだ関係名が
  並ぶ（自分もグループの場合を除く）。容器側からは中身がそれぞれ何なのか
  見え、メンバー側からは自分がどの容器に属するかだけが `グループ:` として
  見えればよい、という設計。
- **小グループ**: 小グループノード自身の そっちにとって にも実際に選んだ
  関係名がそのまま並ぶ（ルール 2 が最初から効かない）。一方でそのメンバー
  自身の こっちにとって からは、相手が小グループでも `グループ:` として
  見える（ルール 1 は小グループにも効く）。

| 状況 | 自分の こっちにとって | 相手の そっちにとって |
|---|---|---|
| ノート → グループ、`なし` | `グループ: [容器]` | `ノート: [自分]` |
| ノート → グループ、`論点` | `グループ: [容器]` | `論点: [自分]` |
| グループ → グループ | `グループ: [親]` | `グループ: [自分]` |
| ノート → 小グループ、`資料` | `グループ: [親]` | `資料: [自分]` |
| 小グループ → 小グループ、`資料` | `グループ: [親]` | `資料: [自分]` |
| ノート → ノート、`なし` | `ノート: [相手]` | `ノート: [自分]` |
| ノート → ノート、`論点` | `論点: [相手]` | `論点: [自分]` |

`ノート:` = 特別な役割の無いメンバー（サブ容器ではない、の意）。

## 4. sync の挙動

`:UpdateMD` / 保存時 AutoSync が `note_format_v2.py` を呼ぶ。

- **書き換えるのは見張り 2 行を持つノートだけ。** 旧 v1（`Parent:` 等）/ 旧 `---` /
  日記 / 素の散文 / front matter `pkm: raw` のノートは **1 バイトも触らない**。
- 上側の `関係: [B]` があれば B の下側にも何らかの行ができる（リンクの存在
  自体は必ず両側に反映される）。ただし**ラベルの文言は自動でミラーしない**
  （下記）。上下どちらを編集しても相方へ反映。`.pkm_sync_state_v2.json` で
  「追加か削除か」を判定。
- `関連` は対称: 片方の下側に足すと相方の下側にも入る（ラベルも `関連:` の
  まま、これは例外）。
- 相互に張り合っている関係は下側に出さない。
- 本文（散文）中のリンクは、型付きの関係が無ければ相手の下側に `バックリンク:` として出る。
- **関係ラベルは方向性を持つことが多い**（例: `きっかけ:` は「相手が自分の
  きっかけだった」という A 視点の言葉で、B からそのまま「きっかけ」と
  ミラーすると意味が通らない）。そのため `関連`（対称）と `グループ`
  （attribute による強制。§3）を除き、ラベルは自動では**そのまま**ミラーしない。
  これは**上側・下側どちらの方向でも同じ**（`zc`/`ca` のように そっちにとって
  側から書いた場合も、`zp`/`at`/`bc` のように こっちにとって 側から書いた
  場合も対称に扱う）:
  - **自分の側に既に何か書かれている**場合（最初から自分で書いた場合も、
    手で書き換えたものも）は、それをそのまま使う（sticky。再パースする
    たびに読み直すだけなので自然にそうなる）。相手側のラベルを後から
    変えても、自分側の既存の行はそのまま。
  - まだ自分の側にその相手向けの行が無い場合、相手側がどう書いたかで
    決まる（**手で打った `:` / `;` の書き分け**）:
    - 相手が **`語:`**（コロン、既定の `ノート:` も含む）で書いていれば、
      **`(語)` の括弧付き**で自動的に反映する（`ノート` 自体は括弧を
      付けずそのまま `ノート:`）。「相手から見た呼び方である」ことを
      括弧で示しつつ、元の言葉も残す。
    - 相手が **`語;`**（セミコロン）で書いていれば、自動では一切反映せず
      素の **`ノート:`** になる（「これは相手には書かない」という意思表示）。
  - `ca`/`at`/`bc`/`cu` で「相手側にも書く」を選ぶと、相手側にその場で
    `(ラベル)` が即座に書き込まれる（§6）。これは上記の「相手が `:` で
    書いた場合の自動反映」を待たずに今すぐ反映させるショートカットに
    過ぎない ── 選ばなくても、自分の側が `:` 終端である限り次の sync で
    同じ結果になる。`\ca`/`\at` は役割が逆で、自分側に `(ラベル)` を書き、
    相手側には質問なしで常に生のラベルを書き込む。
- リンク表示名も同じ考え方で追従を判定する：今書かれている表示名が
  **前回 sync 時点の相手のタイトルと同じ**（＝手で変えていない）なら
  現タイトルへ追従し、**違っていれば**（＝手で変えた）そのまま残す。
  前回の記録が無い場合（初回 sync）は、その時点で表示名がタイトルと違って
  いれば「手で変えたもの」とみなして残す。前回タイトルは
  `.pkm_title_state_v2.json` に記録する。
- **セクション（関係ラベル）の並び順も sticky**: 上側・下側とも、ファイルに
  既にある関係ラベルのブロック順（`グループ:`/`ノート:`/`索引:` 等をどの順で
  並べたか）は sync で強制的に並べ替えない。手でブロックごと並べ替えれば、
  次の sync でもその順のまま保たれる。新しく増えたラベルだけ末尾に追加する。
  （各ラベル内のリンクの並び順も同様に sticky。§4 で既出）

## 5. 旧形式の取り込み

sync は旧形式を触らない。変換は明示的に:

- `:V2Migrate %` … 現在のファイルを v2 化（`Parent:`/`Child:` → `関連:`、
  `[Index]` → `グループ:`、見張り 2 行を付与）
- `:V2Migrate` … PKM ルート全体
- 日記など「絶対に触らせたくない」ものは、そのまま置く（見張りが無い＝凍結）か
  front matter に `pkm: raw`。

merge（両方 14 桁タイムスタンプ）: `note_format_v2.py dupcheck A B` で
同名衝突を確認 → 衝突ファイルを +1 秒に改名 → `diary/` 等へ移動 →
`.pkm_sync_state_v2.json` / `.pkm_title_state_v2.json` 削除 →
変換したいものだけ `:V2Migrate` → `:UpdateMD`。

## 6. コマンド / キー

| キー / コマンド | 動作 |
|---|---|
| `zc`（`:NC`） | 子ノート作成。関係を数字で選び、リンクを **現ノートの そっちにとって 側**へ。相方の こっちにとって 側は sync が生成（`ノート`/`関連` 以外を選ぶと、相方側のラベルをその場で書くか聞かれる。§4） |
| `zp`（`:NP`） | 親ノート作成。リンクを **現ノートの こっちにとって 側**へ。相方の そっちにとって 側は sync が生成（`ノート`/`関連` 以外を選ぶと、相方側のラベルをその場で書くか聞かれる。§4） |
| `zh`（`:NH`） | 新ノート作成、リンクを **カーソル行の直下（本文）**へ。関係に入れない → 相手には `バックリンク:` |
| `zw` | 属性ノート作成。属性ノートは zw でのみ作れる。まず属性の種類を数字ピッカーで選ぶ（今は `1 グループ  2 小グループ`、末尾 = 自由入力。今後増やすときはここに追加するだけ）。`グループ` は関係固定（常に `グループ`、ピッカーを飛ばす）→ そのまま c=子 / p=親。`小グループ`（と自由入力の属性）は続けて関係も数字ピッカーで選ぶ（§2 の「索引」入りの一覧）→ c=子 / p=親。関係も選ばせるのは、`グループ` と違って `小グループ` は自分がソースの時に sync が関係名を上書きしないため（§3） |
| `pe` | 展開。現ノートを起点に 親（こっちにとって）/ 子（そっちにとって）/ 文中（本文中のリンク・双方向）を辿り、集めたノートの本文を 1 つの `_tmp/T_<timestamp>.md` へ集約する。§8 参照 |
| `bc` / `cu` / `at` | `yurii_pkm#v2_add_link()`。関係を数字で選択（末尾 = 自由入力）→ **現ノートの こっちにとって 側**へ 1 行。`グループ`/`関連` は関係の制約でそっちにとって側へ |
| `ca` | 同じく `yurii_pkm#v2_add_link()`。既定は逆で **現ノートの そっちにとって 側**へ 1 行（子リンクの意味）。`グループ`/`関連` は制約でそのままそっちにとって側 |
| `\at` / `\ca` | `at`/`ca` の逆括弧版。向き（こっちにとって / そっちにとって）は同じだが、「質問なしで即座に生のラベルを書く側」と「質問で聞かれる側」が逆になる。相手側には常に（質問なしで）生の関係名をそのまま書き込み、今開いているノート側は「書く？」と聞かれる（§6 参照。`ca`/`at` とは聞かれる側が逆） |
| 関係ピッカー | 数字 + 末尾 = 自由入力。`Esc` / `q` でキャンセル（ファイルを作らない） |
| `:V2Migrate [%]` | 旧形式 → v2 変換（明示） |
| `:UpdateMD` / AutoSync | sync（`note_format_v2.py update` / `update_one`） |
| テンプレート | front matter + H1 + 見張り 2 行のみ |
| 移動系（`,,` / `,.` / `,/` / 番号ジャンプ / 覗き見） | **別途設計**（後日） |

`bc`/`ca`/`cu`/`at` はクリップボード（無名レジスタ）の複数行 / 複数リンクにも対応する:

- 対象が複数件ある場合、まず「関係を一括で同じにするか、一つずつ選ぶか」を聞く
  （対象が全部グループ / 小グループ属性なら、この関係ピッカーも「索引」入りの
  一覧になる）。
- 対象に `attribute: グループ` / `attribute: 小グループ` のファイルが含まれる場合、
  そのリンクの表示名を入力できる（既定値は相手の現タイトルで、そのまま Enter すれば
  従来どおり）。そのような対象が複数件あるときは、先に「一つずつ入力するか、
  入力せずタイトルのまま追加するか」を聞く。
- 1 件だけの場合はこれらの確認を挟まず、今までどおりすぐ関係ピッカー（→ 表示名入力
  が必要なら 1 件だけ）に進む。
- 選んだ関係に `ノート`/`関連`/`索引` 以外（方向性を持ちうる言葉）が 1 件でも
  含まれる場合の扱いは、それがピッカーの選択肢（`補足`/`資料` 等）か、
  自由入力で打った言葉かで変わる:
  - **自由入力**の言葉だけ、「**聞かれる側**にも『その関係名』を `(ラベル)`
    で書く？ 1 はい 2 いいえ（自動）」を**全体で 1 回だけ**聞く（対象ごとに
    聞き直さない）。「いいえ」なら聞かれる側には何も書かず、次の sync が
    既定の `ノート:` を生成する（§4）。
  - **ピッカーの選択肢**（`補足`/`資料` 等）は聞かない。常に**聞かれる側**へ
    `(ラベル)` をその場で即座に書き込む。
  - どちらの場合も「もう一方の側」は常に、質問なしでその関係名をそのまま
    （括弧なしで）その場に書き込む。
  - `ca`/`at`/`bc`/`cu`: 聞かれる側 = **相手側**。自分側は常に生のラベルを
    即座に書く。「はい」なら相手側にも `(ラベル)` を即座に書く（sync を
    待たずに反映される）。「いいえ」なら相手側には何も書かず、次の sync が
    既定の `ノート:` を生成する（§4）。
  - `\ca`/`\at`: 聞かれる側 = **自分側**（今開いているノート）。相手側は
    常に生のラベルを即座に書く。「はい」なら自分側にも `(ラベル)` を即座に
    書く。「いいえ」なら自分側には何も書かず、次の sync が既定の `ノート:`
    を生成する。

## 7. 展開（`pe`、`expand_v2.py`）

現ノートの周辺を 1 つの読み物として集約する使い捨て機能。実装は
`plugin/yurii_PKM/python/expand_v2.py`。旧 v1 時代の `expand_s.py`
（`\se` / `:ExpandLinks`）とは別物 ── あちらは `Parent:`/`Child:`/`Back:`
という v1 の見出し名を前提にしており、v2 の見張りコメント形式には
対応していない（v2 ノートに対して壊れた出力になる）。

**辿る方向:**

- 親 = こっちにとって側の全リンク先
- 子 = そっちにとって側の全リンク先（バックリンクを除く）
- 文中 = 双方向。相手の `バックリンク:`（＝相手が自分を本文で言及）と、
  自分の本文が言及している相手（型付きの関係を持たない素のリンク）の
  どちらも含む

**モード（`pe` を押すと最初に選ぶ）:**

- **シンプル**: 深さ N を 1 つだけ指定。親/子/文中を区別せず、全リンクを
  平等に扱って N 階層まで BFS で辿る（1 つの共有カウンタ）。
- **詳細**: **子が主軸**。起点から純粋な子チェーンを depth=子の深さ まで
  再帰的に展開する（掘り下げるだけ、他の方向へは切り替わらない）。
  そのチェーンに含まれる**各ノートそれぞれ**（起点自身も含む）について、
  そのノートを起点とした純粋な親チェーン（depth=親の深さ）・純粋な文中
  チェーン（depth=文中の深さ）を独立に辿って加える。ここでも種類は
  切り替わらない（親を辿った先でさらに子や文中には進まない）ので、
  親の先がハブ的なノート（Index 等）でも、そのハブの他の子まで展開される
  ことはない。子チェーン上の別々のノートがそれぞれ自分自身の親・文中を
  持ってくる（＝起点の親だけでなく、途中で見つかった子ノート自身の親も
  独立に出てくる）。
  前回入力した 3 つの値は `ROOT/.pkm_expand_prefs.json` に記録され、次回は
  「保存済み設定を使う / 新しく設定する」から選べる。

どちらのモードも、同じノートが複数の経路で見つかっても 1 回だけ集める
（重複除去）。

**出力:** `ROOT/_tmp/T_<timestamp>.md` に、集めた各ノートを

```
## <タイトル>

<本文（front matter・見張りコメント・自分の H1 は除く）>

**関係リンク:**
- <ラベル>: [表示名](相手へのパス)、...

---
```

の形で並べる。**関係リンク**は、そのノートが実際に持つ関係（こっちにとって
/そっちにとって の全ラベル）をそのままリンクとして書き出したもの ──
そこから先の展開はしない。深さの都合で展開されなかった終端ノート（例:
Index のようなハブ）でも、そのノート自身が何にリンクしているかは見える
ようにするため。目次は発見経路に沿って**段落（インデント）**され、各ノート
がどこから（親／子／文中のどの経路で）見つかったかが一目で分かるように
なっている。front matter や見張りコメントを含まないプレーンな読み物に
なるので、そのまま人に見せる用途にも使える。**一方向のスナップショット**
で、このファイルを編集しても元ノートには反映されない。

`_tmp/` は sync が一切スキャンしない（`note_format_v2.EXPAND_TMP_DIR`）。
中身は使い捨てなので、フォルダごと消してよい。

## 8. 実装状況（main へマージ済み）

- 済: パース / レンダ / sync（対称 `関連`・バックリンク・状態追跡）、見張りコメント形式、
  グループ / 小グループ 印（旧 カテゴリー / キーワード からの前方互換込み）、
  `zc`/`zp`/`zh`/`zw`、`v2_add_link`（`bc`/`ca`/`cu`/`at`。複数リンク対応・
  属性ノートの表示名入力・下側表示名の固定化）、展開（`pe`/`expand_v2.py`）、
  `:V2Migrate`、`dupcheck`、v1 デフォルト無変更、
  旧見張り（している/されている）の自動移行
- 未: 移動コマンドの見張り基準への読み替え、グラフビュー、展開の詳細モードの
  実運用フィードバックを踏まえた調整
