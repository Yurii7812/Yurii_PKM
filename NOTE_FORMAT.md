# ノート仕様 v2（草案）

現行の `Parent:` / `Child:` / `BackLink:` 見出しを廃し、**`---` の位置で parent / child を暗に分ける**方式。
`README.md` の「セクションモデル」「ノートテンプレート」を置き換える提案。

---

## 1. 基本

- 1 ファイル = 1 ノート。
- ファイル名は**タイムスタンプのみ**（`20260909141200.md`）。プレフィクスなし、`filetype` なし。
- YAML front matter は `time` と `title` のみ。
- 本文（H1 + 散文）の後に、リンク領域が続く。

## 2. `---` による暗黙の parent / child 分割

見張り行に **HTML コメント**を使う:

- `<!-- している -->` … 上側（このノートが *している* こと）の開始
- `<!-- されている -->` … 下側（*されている* こと）の開始

HTML コメントなので **どの Markdown レンダラでも不可視・見出し化しない**
（`---` だと直前行が Setext 見出しになり文字が巨大化する問題を回避）。
本文には `---` も `ワード:` も自由に書ける。関係セクションとして解釈されるのは
2 つの見張り行より後ろだけ。

| 位置 | 役割 | 旧 | 編集 |
|---|---|---|---|
| `<!-- している -->` 以降 | このノートが *している* こと（能動） | `Parent:` | 手 / sync |
| `<!-- されている -->` 以降 | このノートが *されている* こと（受動） | `Child:` / `BackLink:` | 手 / sync |

どちらも手で編集でき、片面の変更はもう片面へ sync が反映する（§4「対称同期」）。

- 上 = 「自分が世界へ張った線」。どの分類に属し、何を前提にし、何と論じているか。
- 下 = 「世界から入ってきた線」。どこから参照されているか。
- 相互に張り合っている（上に既にある）リンクは、下には出さない。

## 3. 関係(relation)セクション（上側）

### 形式

1 本ならインライン、2 本以上なら `型:` で改行してブロック。

```
型: [title](20260101120000.md)
```

```
型:
[title](20260102120000.md)
[title](20260103120000.md) — 注釈をここに書ける
```

- 型が空になったら行ごと削除する（`論点:` だけ残さない）。
- リンク形式は `[title](タイムスタンプ.md)` 固定。
- 注釈 `— …` は**上側の行にだけ**付けられる。
- 本数に応じたインライン ⇄ ブロックの切り替えは sync が正規化する。手で整形しない。

### 関係の一覧（この 6 つ以外の `語:` 見出しも型として使える＝自由入力）

| 型 | 意味 |
|---|---|
| `所属:` | このノートが入っている容器（トピック索引ノート / index）。有向・上向き。原則 1 つ |
| `見解:` | このノートの立場・意見 |
| `前提:` | 先に理解しておく必要がある概念 |
| `論点:` | 議論・対立・問いの立っている点 |
| `ワード:` | このノートを索引するキーワード（一般語・文脈依存語の両方） |
| `関連:` | 上のどれでもない横のつながり。**対称**：上側に出ず、両ノートの下側に現れる |

- `所属:` = 容器への membership。`論点`/`見解` は問い・主張が端点に要る関係なので
  容器ノート（哲学など）には刺さらない。容器が使うのは `所属` / `ワード` / `関連` だけ。
- `ワード:` = 紙の「Index / トピック Index に並ぶキーワード」。索引語ノートへのリンクを並べる。
- 迷ったら `関連:`。

### 並び順（上側）

`カテゴリー` → `前提` → `論点` → `ワード` → `関連`。
各セクション内は手で重要度順に並べてよい（sync は上側の順を触らない）。

## 4. 下側（されている）

sync が全ノートの上側を走査し、各リンク先の `---` 下に、**型ごとに束ねた逆リンク**を生成する。

```
---
論点:
[title](20260104120000.md)
関連:
[title](20260105120000.md)
ワード:
[title](20260106120000.md)
```

- 上側で相手にリンクしていれば、その相手は下側に出さない（相互リンク済みは除外）。
- 並び: 型順（上と同じ）→ ファイル名昇順。
- **本文（散文）中のリンク**は、両者に型付きの関係が無ければ相手の下側に `バックリンク:` として出る（型セクションには入れない）。型付きの関係が既にあればそちらで表示。

### 下側も編集できる（対称同期）

「上が手書き・下は生成」という固定ではない。**`---` の上下どちらを編集してもよい。**

- 下側に `型: [X]` を足す → sync が X の上側に相方リンクを足す。
- 下側から行を消す → sync が相方（X の上側）からも消す。
- どちらの向きも「片面を編集 → もう片面へ反映」。上側編集も同じ。

判定のため、前回整合した有向関係の集合を PKM ルート直下の
`.pkm_sync_state_v2.json` に保存する（上側だけ / 下側だけに現れる関係が
「追加」か「削除」かを区別するため）。このファイルは手で触らない。

## 5. テンプレート

```markdown
---
time: 2026-09-09 14:12:00
title: 集中と気づきの違い
---

# 集中と気づきの違い

集中しようとするほど雑念が増える。狙いは集中ではなく気づきの側か。
本文には `---` も `ワード:` も自由に書ける。

<!-- している -->
カテゴリー: [瞑想](20250101120000.md)
前提: [注意の分類](20250103090000.md)
論点:
[瞑想のコツがわからない](20250111093000.md) — 集中は対象固定、気づきは対象を選ばない
[サマタとヴィパッサナー](20250105120000.md)
ワード:
[開放的注意](20250120120000.md)
[マインドフルネス](20250121120000.md)
関連: [呼吸法](20250107120000.md)
<!-- されている -->
論点:
[雑念との付き合い方](20250106120000.md)
関連:
[歩行瞑想](20260131120000.md)
```

## 6. プラグイン改修（この仕様に合わせる）

| 項目 | 変更 |
|---|---|
| `Parent:` / `Child:` / `BackLink:` | 廃止 → `---` 境界 + 関係(relation)セクション |
| sync エンジン | `python/note_format_v2.py`。**既定でこれ**。旧 `yurii_pkm_sync.py` は `let g:yurii_pkm_format = 'v1'` を明示した時だけ |
| `bc` / `ca` / `cu` / `at` | `yurii_pkm#v2_add_link()`。型を**数字**で選択（1 カテゴリー / 2 前提 / 3 論点 / 4 見解 / 5 ワード / 6 関連 / 7 入力）→ 上側へ 1 行挿入（1 本＝インライン、2 本目でブロック化）。逆側は書かない |
| `nc`（`:NC`） | 子ノート作成。関係を数字で選び、リンクを **現ノートの されている 側**へ挿入。相方（新ノートの している 側）は sync が生成 |
| `np`（`:NP`） | 親ノート作成。リンクを **現ノートの している 側**へ。相方の されている 側は sync が生成 |
| `nh`（`:NH`） | 新ノートを作り、リンクを **カーソル行の直下（本文）**に挿入。関係セクションに入れない → 相方には `バックリンク:` として出る |
| 関係ピッカー | `1`〜`6` + `7`=自由入力。`Esc` / `q` でキャンセル（キャンセル時はファイルを作らない）。`関連` を選ぶとリンクは常に されている 側へ |
| `at`（旧: 相方の Child にも書く） | v2 では現ノートの上側に足すだけ。相方側は sync が生成 |
| AutoSync / `:UpdateMD` | `note_format_v2.py` を呼ぶ。上下を読んで整合、相互リンク除外、インライン ⇄ ブロック正規化、表示名を現タイトルへ |
| テンプレート（`mm`/`nf`/`nk`/`index`） | front matter + H1 + `---` のみ（型ヘッダなし・`filetype` なし） |
| 移動系（`,,` / `,.` / `,/` / 番号ジャンプ / 覗き見 ほか） | **別途設計**（後日）。`---` を境界とした読み替えを含めて未確定 |

### 実装状況（ブランチ `note-format-v2`）

- 済: `note_format_v2.py`（sync / template / new / **旧 v1 の自動移行**）+ テスト、
  既定を v2 化、テンプレート、`v2_add_link`（`bc`/`ca`/`cu`/`at` から）
- 旧 v1 ノートは初回 sync で自動変換: Parent/Child/Branch のリンク -> `関連:`、
  `[Index](index.md)` -> `所属:`、BackLink は破棄（下側に再生成）。リンクは失わない
- 未: 移動コマンドの `---` 読み替え、`nc`/`nq` の対話フロー細部、v1 テストの移行

## 7. 最小の一手（まず入れる分）

1. `Parent:` → `所属:`（+ 必要なら他の型）に改名。`Child:` 廃止。
2. 下側（されている）を型ごとに束ねて自動生成。相互リンク除外。
3. `<!-- している -->` / `<!-- されている -->` を上側 / 下側の開始として置く（移動コマンドの読み替えは別途）。

型を 5 つ全部いきなり使わず、`所属:` と `ワード:` だけで運用を始めてもよい。
擦り切れたところから `前提:` / `論点:` / `関連:` を足す。

## 8. 既存の日記 / 外部ファイルとの統合

**要点: `<!-- している -->` / `<!-- されている -->` の 2 行はノート作成時（テンプレート）
にしか入らない。sync はこれを持つノートだけを書き換え、持たないファイルには
何も足さない。** だから前のノートは黙って安全。

- 前の日記（素の散文でも、旧 `Parent:`/`Child:` 形式でも）を `g:yurii_pkm_root`
  配下、例えば `diary/` に置いてよい。sync は 1 バイトも触らない。
- 現ノートから日記へ `関連: [日記](diary/xxxx.md)` を張るのは自由。日記側は
  変わらない（`<!-- されている -->` は生えない）。表示名は打った通りに固定
  （管理外ノートへのリンクはタイトル追従しない）。
- 日記の本文に `[PKMノート](../20250104.md)` と書くと、その PKM ノート側の
  `<!-- されている -->` に `バックリンク: [日記名]` が出る（片方向）。
- 日記を graph に完全参加させたいものだけ、明示的に変換する:
  - `:V2Migrate %` … 現在のファイルを v2 化（`Parent:`/`Child:` → `関連:` 等、
    見張り 2 行を付与）
  - `:V2Migrate`   … PKM ルート全体を一括変換
  - sync は決してこの変換を自動でやらない。
- 見張りを持つのに凍結したいファイルは front matter に `pkm: raw`。

### merge の手順（両方 14 桁タイムスタンプの場合）

同じ秒に作られた 2 ファイルだけが衝突しうる（実質ほぼ起きない）。

1. 衝突チェック: `python note_format_v2.py dupcheck OLD_DIARY PKM_ROOT`
   → 出たファイルは取り込む側の名前を +1 秒にずらし、日記内の当該リンクも直す。
2. 日記の `*.md`（`index.md` 以外）を `diary/` サブフォルダへ移動（相対リンクで
   解決される）。日記側の `index.md` は 1 ノートに畳むか本体 index へ統合。
3. `.pkm_sync_state_v2.json` を削除（merge 後の集合から作り直す）。
4. 変換したいものだけ `:V2Migrate`。素の日記はそのまま置いておける。
5. `:UpdateMD` で `<!-- されている -->` とバックリンクが整う。

merge 前に vault を丸ごとコピー / commit しておくと安心。
