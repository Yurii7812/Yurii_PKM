# ノート形式 v2

`Parent:` / `Child:` / `BackLink:` を廃し、**HTML コメントの見張り行**で
「こっちにとって（自分視点）」と「そっちにとって（相手視点）」を分ける方式。

実装: `plugin/yurii_PKM/python/note_format_v2.py` + `autoload/yurii_pkm.vim`。
既定でこれ。旧エンジンに固定したい時だけ `let g:yurii_pkm_format = 'v1'`。

---

## 1. ノート 1 枚の形

```markdown
---
time: 2026-09-09 14:12:00
title: 認識論とは何か
attribute: カテゴリー        ← 任意。容器ノートの印だけ（§3）
---

# 認識論とは何か

本文（散文）。ここには `---` も `ワード:` も何でも書ける。

<!-- こっちにとって -->
所属: [哲学](20250101120000.md)
前提: [知識の定義](20250103120000.md)
論点:
[普遍は実在するか](20250111120000.md) — 実在論 vs 唯名論
[認識の起源](20250112120000.md)
<!-- そっちにとって -->
論点: [実在論で見るべき](20250120120000.md)
関連: [存在論](20250121120000.md)
バックリンク: [2026-05 の読書メモ](20260501120000.md)
```

- ファイル名 = 14 桁タイムスタンプ。`index.md` だけ別。
- front matter は `time` / `title`（+ 任意で `attribute: カテゴリー`）。
- 本文の後、`<!-- こっちにとって -->` から下が **上側**（こっちにとって＝このノートにとって、そのノートが○○）。
- `<!-- そっちにとって -->` から下が **下側**（そっちにとって＝そのノートにとって、このノートが○○）。
- 見張りは **HTML コメント**なので、どの Markdown レンダラでも不可視・
  見出し化しない・本文の `---` や `語:` 行と衝突しない。
- **見張り 2 行はノート作成時（テンプレート）にだけ入る。sync は後から足さない。**

## 2. 関係（relation）

見張りの下に入るセクション。1 行 1 リンク。

ピッカー: `1 キーワード  2 ノート  3 関連  4 補足  5 資料  6 入力`

`カテゴリー` はピッカーに出さない（選べると向きが固定されて壊れる。§3参照）。

| 関係 | 向き | 意味 |
|---|---|---|
| `カテゴリー:` | こっちにとって のみ | 容器ノートへの所属。ピッカーでは選べず、相手が `attribute: カテゴリー` なら sync が**自動でこのラベル**にする |
| `キーワード:` | そっちにとって のみ | このノートを索引する語。選ぶと**語そのものを入力**する（相手のタイトルは使わない） |
| `ノート:` | 両方 | 既定。特別な役割のないリンク |
| `関連:` | 対称 | 横のつながり（両ノートの下側に出る） |
| `補足:` | 両方 | 本体を補う説明 |
| `資料:` | 両方 | 出典・参考にしたもの |
| 他の `語:` | 両方 | 自由入力 |

- **論点 / 見解 / 前提 は廃止**した（紙の PKM では論点・見解で分けず、キーワードで
  索引する）。既存ノートの `論点:` / `見解:` / `前提:` はそのまま残り、sync の
  並び替えも従来どおり効く。ピッカーから選べなくなっただけ。
- 向きの制約はピッカーの選択が優先する。`np`（親）で `キーワード` を選べば
  **そっちにとって 側**へ入る。
- キーワード入力中は、どのノートに足しているか見失わないよう
  `[そのノートのタイトル] キーワード: ` の形でプロンプトにタイトルを出す。

## 3. ノード属性（`attribute: カテゴリー` / `attribute: キーワード`）

容器ノート（カテゴリー）・索引ノード（キーワード）の印。`カテゴリー` は `nk` でのみ
作れる（c=子 / p=親 を聞く）。`キーワード` を含め、後から他のノートを属性化する
専用コマンドは無い（front matter に手で `attribute: キーワード` を足せば効く）。

`カテゴリー:` / `キーワード:` ラベルは、選んだ関係に関わらず 2 つの自動ルールで
上書きされる（値は attribute に書いた方に従う。カテゴリーとキーワードで規則は同じ）:

1. 相手が `attribute: X` → 自分の こっちにとって 側が `X: [相手]`
2. 自分が `attribute: X` → 相手の そっちにとって 側が `X: [自分]`（サブ容器 / サブ索引として）

この 2 ルールはわざと非対称: 容器・索引ノード自身の そっちにとって 側には
（自分もその属性を持つ場合を除き）実際に選んだ関係名がそのまま並ぶ ──
容器側からは中身がそれぞれ何なのか見え、メンバー側からは自分がどの容器・
キーワードに属するかだけが見えればよい、という設計。

| 状況 | 自分の こっちにとって | 相手の そっちにとって |
|---|---|---|
| ノート → カテゴリー、`なし` | `カテゴリー: [容器]` | `ノート: [自分]` |
| ノート → カテゴリー、`論点` | `カテゴリー: [容器]` | `論点: [自分]` |
| カテゴリー → カテゴリー | `カテゴリー: [親]` | `カテゴリー: [自分]` |
| ノート → キーワード、`論点` | `キーワード: [語]` | `論点: [自分]` |
| キーワード → キーワード | `キーワード: [親]` | `キーワード: [自分]` |
| ノート → ノート、`なし` | `ノート: [相手]` | `ノート: [自分]` |
| ノート → ノート、`論点` | `論点: [相手]` | `論点: [自分]` |

`ノート:` = 特別な役割の無いメンバー（サブ容器・サブ索引ではない、の意）。

`キーワード` はこの attribute とは別に、ピッカーの通常の関係としても存在する
（§2）。そちらは「索引する語を自由入力する」既存の用途のままで、attribute の
有無とは無関係に動く。

## 4. sync の挙動

`:UpdateMD` / 保存時 AutoSync が `note_format_v2.py` を呼ぶ。

- **書き換えるのは見張り 2 行を持つノートだけ。** 旧 v1（`Parent:` 等）/ 旧 `---` /
  日記 / 素の散文 / front matter `pkm: raw` のノートは **1 バイトも触らない**。
- 上側の `関係: [B]` ⟺ B の下側に `関係: [A]`（同じ関係名）。上下どちらを
  編集しても相方へ反映。`.pkm_sync_state_v2.json` で「追加か削除か」を判定。
- `関連` は対称: 片方の下側に足すと相方の下側にも入る。
- 相互に張り合っている関係は下側に出さない。
- 本文（散文）中のリンクは、型付きの関係が無ければ相手の下側に `バックリンク:` として出る。
- リンク表示名は相手の現タイトルへ追従（管理外ノートへのリンクは打った通り固定）。

## 5. 旧形式の取り込み

sync は旧形式を触らない。変換は明示的に:

- `:V2Migrate %` … 現在のファイルを v2 化（`Parent:`/`Child:` → `関連:`、
  `[Index]` → `所属:`、見張り 2 行を付与）
- `:V2Migrate` … PKM ルート全体
- 日記など「絶対に触らせたくない」ものは、そのまま置く（見張りが無い＝凍結）か
  front matter に `pkm: raw`。

merge（両方 14 桁タイムスタンプ）: `note_format_v2.py dupcheck A B` で
同名衝突を確認 → 衝突ファイルを +1 秒に改名 → `diary/` 等へ移動 →
`.pkm_sync_state_v2.json` 削除 → 変換したいものだけ `:V2Migrate` → `:UpdateMD`。

## 6. コマンド / キー

| キー / コマンド | 動作 |
|---|---|
| `nc`（`:NC`） | 子ノート作成。関係を数字で選び、リンクを **現ノートの そっちにとって 側**へ。相方の こっちにとって 側は sync が生成 |
| `np`（`:NP`） | 親ノート作成。リンクを **現ノートの こっちにとって 側**へ。相方の そっちにとって 側は sync が生成 |
| `nh`（`:NH`） | 新ノート作成、リンクを **カーソル行の直下（本文）**へ。関係に入れない → 相手には `バックリンク:` |
| `nk` | カテゴリーノート作成。c=子 / p=親 を聞くだけ（関係ピッカー無し、常に `カテゴリー`）。カテゴリーは nk でのみ作れる |
| `nw` | 属性ノート作成。まず属性の種類を数字ピッカーで選ぶ（今は `1 キーワード`、末尾 = 自由入力。今後増やすときはここに追加するだけ）→ nk と同じく c=子 / p=親 を聞く |
| `bc` / `cu` / `at` | `yurii_pkm#v2_add_link()`。関係を数字で選択（末尾 = 自由入力）→ **現ノートの こっちにとって 側**へ 1 行。`キーワード`/`関連` は関係の制約でそっちにとって側へ |
| `ca` | 同じく `yurii_pkm#v2_add_link()`。既定は逆で **現ノートの そっちにとって 側**へ 1 行（子リンクの意味）。`キーワード`/`関連` は制約でそのままそっちにとって側 |
| 関係ピッカー | 数字 + 末尾 = 自由入力。`Esc` / `q` でキャンセル（ファイルを作らない） |
| `:V2Migrate [%]` | 旧形式 → v2 変換（明示） |
| `:UpdateMD` / AutoSync | sync（`note_format_v2.py update` / `update_one`） |
| テンプレート | front matter + H1 + 見張り 2 行のみ |
| 移動系（`,,` / `,.` / `,/` / 番号ジャンプ / 覗き見） | **別途設計**（後日） |

## 7. 実装状況（main へマージ済み）

- 済: パース / レンダ / sync（対称 `関連`・バックリンク・状態追跡）、見張りコメント形式、
  カテゴリー印、`nc`/`np`/`nh`/`\pc`、`v2_add_link`（`bc`/`ca`/`cu`/`at`）、
  `:V2Migrate`、`dupcheck`、v1 デフォルト無変更、旧見張り（している/されている）の
  自動移行、pytest 非依存テスト 43 件
- 未: 移動コマンドの見張り基準への読み替え、グラフビュー、`ca` からの非対称ラベル
