# 移動の設計メモ — gs と Space

いま感じている「移動しづらさ」を、検索（`gs`）とリンク移動（`<Space>`）に分けて
問題 → 原因 → 提案 で整理したもの。最後に、キー割り当てでは解決しない根本の話。

---

## gs — ファイル検索

全ノートから名前で直行する手段。長距離移動の担当。

### 01. 結局いつも矢印キーで移動している

- **問題** — 1件動かすのに `↑` `↓` に手が伸びる。ホームローから手が離れて遅い。
- **原因** — 入力モードでは打鍵が全部検索語に行くので `j` `k` が使えない。そのうえ
  `^J` `^K` を**ページ送り**に割り当ててしまったので、いちばん使う「1件移動」が
  Ctrl コードから追い出され、矢印しか残っていない。
- **提案** — `^J` `^K`（`^N` `^P`）を **1件移動** に、ページ送りは `^F` `^B` へ移す。
  fzf・telescope・vim のコマンドライン・Emacs は全部この配置で、「打ちながら動く」
  問題はとっくに Ctrl コードで解決済みの問題だった。

| キー | いま | 直す |
|---|---|---|
| `^J` `^K` | ページ送り | **1件移動** |
| `^F` `^B` | — | **ページ送り** |
| `↑` `↓` | 1件移動（実質これしかない） | 残すが、使わなくてよくなる |

### 02. 数字を打つのにモード切替が要る

- **問題** — 番号で飛びたいのに、まず `<Tab>` で選択モードに入らないといけない。
  面倒だから使わなくなる。
- **原因** — ノート名がタイムスタンプなので**検索語に数字が要る**。数字での行選択と
  正面衝突するため、モーダルにするしかなかった。
- **提案** — 01 を直すとモード切替が**常用パスから消える**。入力モードのまま
  `^J` `^K` で選んで `⏎` で開けるので、`<Tab>` は「番号で一発で飛びたい時だけの
  任意機能」に格下げできる。衝突自体は消せないが、踏まなくて済むようになる。

```
打つ → 絞り込み ・ ^J ^K → 1件 ・ ⏎ → 開く   （Tab は任意）
```

### 03. 開いた先にハイライトが残って `:noh` でも消えない — 解決済み

- **原因** — 結果を開くときに、そのバッファへ `matchadd()` を貼り `@/` まで
  書き換えていた。`matchadd` は `:noh` の管轄外なので消せなかった。
- **対応** — バッファ側のハイライトと `@/` の書き換えを撤去。強調は**ポップアップ内
  （一覧・プレビュー）だけ**を text-property で行う。開いた先には何も残らず、
  最初のヒット行へカーソルが寄るだけ。

---

## Space — parent / child 移動

いま居るノートの関係リンクを辿る手段。近距離移動と探索の担当。

### 01. `h` `l` がとくにしっくりこない — 対応済み

- **原因** — 横キーに**縦移動（10件の区間送り）**を割り当てていた。parent/child は
  実際ほぼ10件未満なので**ほぼ常に死にキー**だった。
- **対応** — 区間送りは廃止。`l` = 奥へ潜る / `h` = 戻る にして、**グラフを辿る＝横移動**
  にした（ranger / miller columns と同じ体系）。件数に関係なく必ず反応する。

### 02. 開くまでの手数が多い — 対応済み

- **原因** — 数字を「1回目は見せる・2回目で開く」の二段階にしていた（`gs` の都合を
  そのまま持ち込んだもの）。
- **対応** — ラベルキー（`1`〜`0` のあと英字）**1タップでその行に潜る**。潜っても
  そのノートはアンカー行（`◎`）とプレビューに出続け、`⌫` / `h` で即戻れる。
  つまり潜るのは非破壊なので、確認のための1打は純粋な無駄だった。
  着地したいときは潜った直後に `⏎`（アンカーが選ばれているので `4` `⏎` の2打）。

### 03. 開くとポップアップが消えるので探索が続かない — 対応済み

- **原因** — 「リンクを1個選ぶダイアログ」として作ったから。選び終わったら用済み、
  という前提だった。
- **対応** — **ウォークモード**にした。潜ると一覧が飛び先の 文中 / している / されている
  に差し替わり、ポップアップは出続ける。`readfile` で読むだけで**バッファは開かない**ので
  同期も autocmd も走らず1ホップが実質ゼロコスト。何ホップ覗いても `⎋` で一括取り消し、
  元のノート・カーソル位置へ戻る。タイトルのパンくずで迷子を防ぐ。

```
ローカル   ラベル 潜る ・ jk 選択 ・ ⏎ 開く ・ ⌫ 戻る ・ ␣ ここ ・ cp 子親 ・ / 絞込 ・ ⇥ 検索
グローバル 打つ ・ ↑↓ 選択 ・ →/⇥ 潜る ・ ⏎ 開く ・ ⌫ 消す/戻る ・ +- 子親
```

文中リンクも同じ一覧に `¶` 付きで載る。1行目は常にアンカー `◎`（いま潜っている
ノート）で、`c` / `p` のリンク追加はここに効く。

### 04. ローカルとグローバルで体系が違った — 対応済み

- **原因** — ローカルはラベルで即潜る・`/` でゲートした絞り込み・`^F^B` プレビュー、
  グローバルはラベル無し・常時入力、と別々に生えた。とくにグローバルで
  **キーで選択できない**のが致命的だった。
- **対応** — スコープ（ローカル / グローバル）は *候補が何か* だけ、サブモード
  （入力 / 選択）は *打鍵がどこへ行くか* だけの違いに整理し、キーを1体系に統一。
  ラベルキーは**常に選択だけ**（潜る/開くは必ず `l` / `⏎`）。Ctrl は全廃し
  プレビュー送りは `f` / `b`。

### 06. モードがあること自体が使いづらさの正体だった — 対応済み

- **原因** — 「入力サブモード / 選択サブモード」を持ち込んだこと。同じキーが
  居場所によって別の意味になるので、**今どっちに居るか**を常に覚えていないといけない。
  01〜05 の不満は全部ここに帰着していた（グローバルでラベルが効かない／検索の
  モード往復／1タップで潜れない）。キー割り当てをいくら直しても消えない。
- **対応** — モードを廃止し、**2つのツール**にした。ツールの中では 1キー＝1意味。
  - **ローカル**（今のノートのリンク）… 打たない。全部コマンド。ラベル1タップで潜る。
  - **グローバル**（全ノート）… 打つ。ラベルを置かないので**数字もクエリに使える**
    （NAVIGATION の出発点だった「タイムスタンプと数字ジャンプの衝突」が構造的に消えた）。
  - `⇥` は「もう一方のツールへ」。グローバル→ローカルは選択ノートへ潜ることを意味する。
  - リンク操作はグローバルでも使えるよう記号（`+` `-` `*` `=`）に置いた。英字を食わない。
  - 見分けは上のバー1行。`◎ ノート名`＝打たない / `検索 …▏`＝打つ。
  - 例外は `/` の一時絞り込みだけ。明示的に入って `⏎`/`⎋` で抜ける（vim の `/` と同じ）。

### 05. 検索したノートを child にするのに往復していた — 対応済み

`⇥` で全ノート検索 → 絞る → `⏎` → ラベルで選択 → `c` で**アンカーの子**として追加。
ポップアップを出たり入ったりしない。`m` で複数マークしてまとめて `c` も可。
関係は数字ピッカー（カテゴリー / キーワード / 前提 / ノート / 関連 / 入力）で選ぶ。

---

## 根本 — キー割り当てでは解決しない話

普段の移動を種類別に並べると、**頻度が高いのに専用の手段がないもの**が2つある。
ポップアップ内のキーをいくら磨いても、この2つは1ミリも速くならない。

| 移動 | 頻度 | いまの手段 |
|---|---|---|
| **直前のノートに戻る／往復する** | 最多 | `⌫` は戻れるが**進めない**ので往復にならない |
| **ハブ（Index・カテゴリー）へ直行** | 多 | 毎回 parent を辿るか検索。3〜6打＋読む |
| 1ホップ先の parent/child | 多 | `<Space>`（上で改善） |
| 名前を知っている特定ノート | 中 | `gs`。4〜6打でほぼ最適 |
| 知らない先を探索 | 中 | ウォークモードで解決予定 |

### 1. 直前のノートとのトグルを1キーに

理論上いちばん速い移動は「選ばなくていい移動」。押すたびに A→B→A→B と往復する。
読むものゼロ・選ぶものゼロ・1打。これ以上速い移動は存在しない。
vim の `^^`（alternate buffer）に相当するものをノート用に。

### 2. 履歴を前にも進めるようにする

`⌫` に対する「進む」がないのが地味に効いている。**戻りが可逆でないと、人は戻るのを
躊躇する**。だから慎重に移動するようになり、結果として全体が遅くなる。前進を足す
だけで戻る操作が気軽になり、探索そのものが速くなる。

### 3. ハブに固定キーを割り当てる

Index や進行中のカテゴリーなど、戻る先は数個に固定されているはず。`\1`〜`\9` の
ような常設キーなら、どこからでも1〜2打・読むものゼロ。ブラウザのブックマークバーと
同じ理屈で、訪問の8割を占める2割のノートに効く。

### 4. 並びを頻度順に固定する

`2` が毎回違うノートだと、毎回リストを読む必要がある。頻度順で固定すれば `1` `2` が
いつも同じ相手になり、読まずに指が動くようになる。地味だが、決める時間がゼロに
なるので効果は大きい。

### 5. プレビューを厚くする

確認だけが目的の移動が消える。移動コストの最適化ではなく**移動回数そのもの**を
減らす方向。いちばん効率的な移動は、しなくて済む移動。

---

## 実装状況

全部入った。

| # | 施策 | キー |
|---|---|---|
| 1 | 直前ノートとの1キー往復 | `_` |
| 2 | 履歴の前進 | `<S-BS>` / `\.` |
| 3 | ハブ固定キー | `\1`〜`\9`（`\H` 登録 / `\0` 一覧） |
| 4 | 並びを最近訪問順に固定 | グローバルスコープの既定 |
| 5 | プレビューを厚く | 全画面2ペイン |
| gs 01/02 | 矢印・モード往復 | 入力サブモードで `↑↓`=選択 / `→`=潜る / `⏎`=開く。Ctrl 廃止 |
| gs 全体 | `gs` と `<Space>` の統合 | 同じエンジン。`gs` = グローバルで開くだけ |
